<?php

function detectAnagrams($word,  $list = array()) : array
{
    $letters = array();
    $anagrams = array();

    foreach(str_split($word) as $letter){
        foreach($list as $){
            
        }
    }

    return $anagrams;
}